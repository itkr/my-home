export type Capabilities = {
  lights: {
    available: number;
    total: number;
  };
  sensors: {
    available: number;
    total: number;
  };
  groups: {
    available: number;
    total: number;
  };
  scenes: {
    available: number;
    total: number;
  };
  rules: {
    available: number;
    total: number;
  };
  schedules: {
    available: number;
    total: number;
  };
  resourcelinks: {
    available: number;
    total: number;
  };
};
